export interface AutoCompleteData {
  brands: { name: string; preOrderBrandId: number }[];
  models: { name: string; preOrderBrandId: number }[];
}

export interface Options {
  brands: { name: string; preOrderBrandId: number }[];
  models: { name: string; preOrderBrandId: number }[];
}
